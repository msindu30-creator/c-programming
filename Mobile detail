#include <stdio.h>

struct mobile {
    char brand[20];
    char model[20];
    int price;
};

int main() {
    struct mobile m;
    scanf("%s%s%d", m.brand, m.model, &m.price);
    printf("Brand: %s\nModel: %s\nPrice: %d", m.brand, m.model, m.price);
    return 0;
}
