#include <stdio.h>

struct book {
    int id;
    char title[20];
    char author[20];
};

int main() {
    struct book b;
    scanf("%d%s%s", &b.id, b.title, b.author);
    printf("ID: %d\nTitle: %s\nAuthor: %s", b.id, b.title, b.author);
    return 0;
}
