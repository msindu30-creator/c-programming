#include <stdio.h>
#include <string.h>

int main() {
    char pwd[20];

    scanf("%s", pwd);

    if (strlen(pwd) >= 6)
        printf("Valid Password");
    else
        printf("Invalid Password");

    return 0;
}
