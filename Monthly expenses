#include <stdio.h>

int main() {
    float exp, total = 0;
    int i;

    for (i = 1; i <= 30; i++) {
        scanf("%f", &exp);
        total += exp;
    }

    printf("Monthly expenses = %.2f", total);
    return 0;
}
