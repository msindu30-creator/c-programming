#include <stdio.h>

int main() {
    char str[50];
    int i, count = 0;

    scanf("%s", str);
    while (str[count] != '\0')
        count++;

    printf("Number of characters = %d", count);
    return 0;
}
