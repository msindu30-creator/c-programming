#include <stdio.h>
int main(){
int i,t[7],max,min;
for(i=0;i<7;i++) scanf("%d",&t[i]);
max=min=t[0];
for(i=1;i<7;i++){
if(t[i]>max) max=t[i];
if(t[i]<min) min=t[i];
}
printf("Highest = %d\nLowest = %d",max,min);
return 0;
}
